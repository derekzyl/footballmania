{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "paths": {
      "@src/*": [
        "./src/*"
      ], // Replace "src" with your source directory
      "@*": [
        "./src/*"
      ] // Optional: Define specific subdirectories
    }
  }
}